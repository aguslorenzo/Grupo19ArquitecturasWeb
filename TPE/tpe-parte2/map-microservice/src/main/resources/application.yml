spring:
  application:
    name: map-microservice

  config:
    import: optional:configserver:http://localhost:8081

  datasource:
    url: jdbc:h2:mem:db-scooter
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8088
