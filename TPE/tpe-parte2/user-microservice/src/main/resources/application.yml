spring:
  application:
    name: user-microservice

  datasource:
    url: jdbc:h2:mem:db-user
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8084
