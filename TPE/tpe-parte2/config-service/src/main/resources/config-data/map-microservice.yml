server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: map-microservice
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/map?createDatabaseIfNotExist=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    leaseRenewalIntervalInSeconds: 30  # Tiempo de renovación de la instancia en segundos
    leaseExpirationDurationInSeconds: 90  # Tiempo de expiración en segundos si no recibe renovaciones