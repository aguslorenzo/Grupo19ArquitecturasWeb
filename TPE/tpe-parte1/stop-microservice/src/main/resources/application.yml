spring:
  application:
    name: stop-microservice

  datasource:
    url: jdbc:h2:mem:db-stop
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console